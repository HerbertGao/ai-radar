# Node / TypeScript
node_modules/
dist/
build/
*.tsbuildinfo
.npm/
.pnpm-store/

# Env & secrets
.env
.env.*
!.env.example

# Cloudflare Tunnel 凭据（locally-managed 隧道的 credentials JSON；config.yml 可入库、凭据不入库）
cloudflared/credentials.json
cloudflared/*.json

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Test / coverage
coverage/
.vitest/

# OS / editor
.DS_Store
.idea/
*.swp

# Claude Code local-only settings
.claude/settings.local.json
.claude/**/settings.local.json

# Playwright MCP 工作目录（抓取/数据核对时的临时产物）
.playwright-mcp/

# 本地 AI 工具目录（Codex / OpenCode 的 skills / commands）
.codex/
.opencode/

# impeccable design-skill local cache（任意层级）
.impeccable/

# pnpm (not used by this project)
pnpm-lock.yaml
pnpm-workspace.yaml
